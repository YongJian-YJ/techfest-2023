{
    "builds": [{
        "src": "TechFest/core/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "TechFest/core/wsgi.py"
        }
    ]
}